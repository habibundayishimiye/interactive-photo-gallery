// // Select all elements with the class "image-container" and store them in a NodeList
// const imageContainers = document.querySelectorAll('.image-container');

// // Loop through each element in the NodeList
// imageContainers.forEach(container => {
//     // Add an event listener for the 'mouseover' event
//     container.addEventListener('mouseover', function() {
//         // Add the 'hovered' class to the current element when the mouse is over it
//         this.classList.add('hovered');
//     });

//     // Add an event listener for the 'mouseout' event
//     container.addEventListener('mouseout', function() {
//         // Remove the 'hovered' class from the current element when the mouse leaves it
//         this.classList.remove('hovered');
//     });
// });
